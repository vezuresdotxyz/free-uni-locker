export async function lockNFT(tokenId: number, lockDuration: number, feeCollector: string) {
  // Contract interaction code will go here
}

/**
 * Get lock information for a specific NFT
 * @param tokenId - The NFT token ID
 * @returns Lock information including owner, unlock time, and fee collector
 */
export async function getLockInfo(tokenId: number) {
  // Contract interaction code will go here
}

/**
 * Get all locked token IDs for a specific owner
 * @param ownerAddress - The owner's address
 * @returns Array of locked token IDs
 */
export async function getLockedTokenIds(ownerAddress: string) {
  // Contract interaction code will go here
}

