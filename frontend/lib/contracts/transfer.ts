export async function transferLock(tokenId: number, recipient: string) {
  // Contract interaction code will go here
}

/**
 * Check if a locked position can be transferred
 * @param tokenId - The NFT token ID
 * @returns Boolean indicating if the position can be transferred
 */
export async function canTransfer(tokenId: number) {
  // Contract interaction code will go here
}

