export async function unlockNFT(tokenId: number) {
  // Contract interaction code will go here
}

/**
 * Check if an NFT is eligible for withdrawal
 * @param tokenId - The NFT token ID to check
 * @returns Boolean indicating if the NFT can be withdrawn
 */
export async function canWithdraw(tokenId: number) {
  // Contract interaction code will go here
}

